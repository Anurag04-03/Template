//Pair
pair<int, int> p = {1,7};
cout << p. first << endl; // outputs 1
cout << p.secon << endl; // outputs 7

// Some Useful Vectors
v. begin(), v.end();
v. push_back(val), v.pop_back();
v. empty(), v. size();
v. insert(it, val), v. erase(it);
v. clear();